<section class="mx-auto mt-16 w-[95%] md:w-[90%]">
  <div class="glass rounded-2xl p-4 overflow-hidden">
    <div class="whitespace-nowrap [animation:marquee_18s_linear_infinite]">
      {['Acme','Globex','Umbrella','Initech','Stark','Wayne','Wonka'].map((b) => (
        <span class="mx-8 inline-block text-white/70">{b}</span>
      ))}
      {['Acme','Globex','Umbrella','Initech','Stark','Wayne','Wonka'].map((b) => (
        <span class="mx-8 inline-block text-white/70">{b}</span>
      ))}
    </div>
  </div>
</section>

<style is:global>
@keyframes marquee {
  from { transform: translateX(0); }
  to   { transform: translateX(-50%); }
}
</style>