<section id="showcase" class="mx-auto mt-24 w-[95%] md:w-[90%]">
  <h2 class="text-2xl font-bold md:text-3xl">Casos</h2>
  <div class="mt-6 grid gap-6 md:grid-cols-2">
    {[
      {title:"Soporte y consultoría", badge:"Automatización 24/7"},
      {title:"Análisis de llamadas", badge:"Insights en minutos"}
    ].map(c => (
      <article class="glass card-hover rounded-2xl p-5">
        <span class="rounded-full bg-sky-400/20 px-3 py-1 text-xs">{c.badge}</span>
        <h3 class="mt-3 text-lg font-semibold">{c.title}</h3>
        <p class="mt-2 text-white/75">Descripción breve del impacto y métricas clave.</p>
      </article>
    ))}
  </div>
</section>